# OCR 문자인식 시스템 개발계획서

**프로젝트명:** SmartOCR — 사진·영상 기반 문자인식 플랫폼  
**작성일:** 2026-05-08  
**버전:** 1.1 (자체 테스트 전략 추가)

---

## 1. 프로젝트 개요

### 1.1 목적
사진 업로드 또는 카메라 실시간 영상에서 문자(텍스트)를 자동으로 추출·인식하는 OCR 플랫폼을 개발한다.  
OCR 코어 엔진을 먼저 구축하고, 이를 REST API로 노출한 뒤 Web 및 모바일 앱으로 단계적으로 확장한다.

### 1.2 목표
- 한국어·영어 등 다국어 문자 인식 지원
- 이미지 파일 업로드 및 카메라 실시간 스트림 처리
- 상용화 가능한 오픈소스만 사용 (MIT, Apache 2.0, BSD 라이선스)
- 확장 가능한 마이크로서비스 아키텍처
- **각 Phase 완료 시 자체 테스트 통과를 배포 게이트로 적용**

---

## 2. 라이선스 정책

> **원칙:** GPL/AGPL 계열 라이선스는 상용 제품에 적용 시 소스코드 공개 의무가 발생하므로 사용 금지.  
> Apache 2.0, MIT, BSD 라이선스만 채택.

| 구분 | 라이브러리 | 라이선스 | 비고 |
|------|-----------|---------|------|
| OCR 엔진 | PaddleOCR | Apache 2.0 | 한국어 포함 80+ 언어 |
| 보조 OCR | EasyOCR | Apache 2.0 | 멀티언어 대안 |
| 딥러닝 추론 | ONNXRuntime | MIT | 경량 배포용 |
| 이미지 처리 | OpenCV | Apache 2.0 | |
| 이미지 처리 | Pillow | HPND(MIT 호환) | |
| API 서버 | FastAPI | MIT | |
| ASGI 서버 | Uvicorn | MIT | |
| 태스크 큐 | Celery + Redis | MIT / BSD | 비동기 처리 |
| 데이터베이스 | PostgreSQL | PostgreSQL License | 상용 무제한 |
| ORM | SQLAlchemy | MIT | |
| 컨테이너 | Docker / Docker Compose | Apache 2.0 | |
| 웹 프론트 | React + TypeScript | MIT | |
| 모바일 | React Native | MIT | iOS/Android 공용 |
| 카메라(모바일) | react-native-vision-camera | MIT | |
| **테스트 — Python** | pytest + pytest-cov | MIT | 코어·API 단위/통합 테스트 |
| **테스트 — Python** | pytest-asyncio | MIT | 비동기 API 테스트 |
| **테스트 — Python** | httpx | BSD | API E2E HTTP 클라이언트 |
| **테스트 — Python** | Locust | MIT | API 부하 테스트 |
| **테스트 — Python** | Hypothesis | MPL-2.0\* | 프로퍼티 기반 테스트 |
| **테스트 — Web** | Vitest | MIT | React 단위/통합 테스트 |
| **테스트 — Web** | React Testing Library | MIT | 컴포넌트 동작 테스트 |
| **테스트 — Web** | Playwright | Apache 2.0 | 브라우저 E2E 테스트 |
| **테스트 — Mobile** | Jest | MIT | RN 단위 테스트 |
| **테스트 — Mobile** | React Native Testing Library | MIT | 컴포넌트 테스트 |
| **테스트 — Mobile** | Detox | MIT | iOS/Android E2E 테스트 |

> \* Hypothesis의 MPL-2.0은 **파일 단위 공개 의무**로, 프로젝트 소스 전체 공개 의무 없음. 테스트 파일에만 사용하므로 상용화 무방.

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        Client Layer                         │
│   ┌──────────────────┐       ┌──────────────────────────┐  │
│   │   Web App        │       │    Mobile App             │  │
│   │  (React/TS)      │       │   (React Native)          │  │
│   │  파일 업로드      │       │  카메라 실시간 스캔        │  │
│   └────────┬─────────┘       └────────────┬─────────────┘  │
└────────────┼────────────────────────────────┼───────────────┘
             │            HTTPS / WebSocket   │
┌────────────▼────────────────────────────────▼───────────────┐
│                      API Gateway Layer                       │
│   ┌─────────────────────────────────────────────────────┐  │
│   │            FastAPI (REST + WebSocket)                │  │
│   │   /ocr/image   /ocr/stream   /ocr/batch             │  │
│   └──────────┬──────────────────────────────────────────┘  │
└──────────────┼──────────────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────────────┐
│                      Core Engine Layer                       │
│  ┌───────────────┐  ┌──────────────┐  ┌──────────────────┐ │
│  │ Preprocessor  │  │  OCR Engine  │  │  Postprocessor   │ │
│  │ (OpenCV)      │→ │ (PaddleOCR / │→ │  (정렬·정제)     │ │
│  │ 노이즈제거     │  │  ONNXRuntime)│  │  JSON 출력       │ │
│  └───────────────┘  └──────────────┘  └──────────────────┘ │
└─────────────────────────────────────────────────────────────┘
               │
┌──────────────▼──────────────────────────────────────────────┐
│                    Infrastructure Layer                      │
│   PostgreSQL  │  Redis (캐시/큐)  │  S3-호환 스토리지        │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 개발 단계 (Phase)

### Phase 1 — OCR 코어 엔진 (6주)

#### 4.1.1 목표
독립 실행 가능한 OCR 파이프라인 구축. CLI·Python API로 테스트 가능한 수준.

#### 4.1.2 디렉토리 구조
```
ocr_model/
├── core/
│   ├── preprocessor.py      # 이미지 전처리
│   ├── engine.py            # OCR 추론 (PaddleOCR wrapper)
│   ├── postprocessor.py     # 텍스트 정제·정렬
│   └── pipeline.py          # 전처리→추론→후처리 통합
├── models/                  # 다운로드된 모델 가중치 (gitignore)
├── tests/
│   ├── conftest.py              # 공통 fixture (샘플 이미지 로드 등)
│   ├── unit/
│   │   ├── test_preprocessor.py # 전처리 단위 테스트
│   │   ├── test_engine.py       # 엔진 단위 테스트 (모델 mock)
│   │   └── test_postprocessor.py
│   ├── integration/
│   │   └── test_pipeline.py     # 전체 파이프라인 통합 테스트
│   ├── accuracy/
│   │   └── test_benchmark.py    # 정확도 벤치마크 (실제 모델 필요)
│   └── fixtures/
│       ├── sample_ko.jpg        # 한국어 샘플 이미지
│       ├── sample_en.jpg        # 영어 샘플 이미지
│       ├── sample_noisy.jpg     # 노이즈 이미지
│       ├── sample_skewed.jpg    # 기울어진 이미지
│       └── ground_truth.json    # 정답 텍스트 레이블
├── scripts/
│   ├── download_models.py       # 모델 자동 다운로드
│   └── run_tests.sh             # 전체 테스트 실행 스크립트
├── pytest.ini
├── .coveragerc
└── requirements.txt
```

#### 4.1.3 핵심 기능 목록
| 기능 | 상세 |
|------|------|
| 이미지 전처리 | 노이즈 제거, 이진화, 기울기 보정(deskew), 해상도 정규화 |
| 텍스트 검출 | DB (Differentiable Binarization) 기반 영역 검출 |
| 텍스트 인식 | CRNN 기반 문자 인식, 한국어·영어·숫자 |
| 언어 자동 감지 | 입력 이미지에서 언어 자동 추론 |
| 신뢰도 점수 | 각 텍스트 블록별 confidence 반환 |
| 출력 형식 | JSON (bbox 좌표 + 텍스트 + confidence) |
| 배치 처리 | 복수 이미지 일괄 처리 |
| CLI 도구 | `python -m core.pipeline --input image.jpg` |

#### 4.1.4 테스트 전략 (Phase 1)

**사용 도구:** pytest, pytest-cov, Hypothesis, pytest-benchmark

**테스트 레이어**

| 레이어 | 대상 | 도구 | 커버리지 목표 |
|--------|------|------|-------------|
| 단위 테스트 | preprocessor, engine, postprocessor 각 함수 | pytest | 90%+ |
| 통합 테스트 | pipeline 전체 흐름 (전처리→추론→후처리) | pytest | - |
| 정확도 벤치마크 | 샘플 이미지셋 CER/WER 측정 | pytest-benchmark | 한국어 CER ≤ 5% |
| 프로퍼티 기반 | 이미지 크기·채널 등 경계값 자동 생성 | Hypothesis | - |

**주요 테스트 케이스**

```python
# tests/unit/test_preprocessor.py 예시
def test_deskew_returns_same_shape(sample_image):
    result = deskew(sample_image)
    assert result.shape == sample_image.shape

def test_normalize_output_range(sample_image):
    result = normalize(sample_image)
    assert result.dtype == np.uint8
    assert result.min() >= 0 and result.max() <= 255

def test_denoise_reduces_noise(noisy_image, clean_image):
    result = denoise(noisy_image)
    noise_before = mse(noisy_image, clean_image)
    noise_after  = mse(result, clean_image)
    assert noise_after < noise_before

# tests/integration/test_pipeline.py 예시
def test_pipeline_korean_text(sample_ko_image):
    results = pipeline.run(sample_ko_image)
    assert len(results) > 0
    assert all("text" in r and "confidence" in r for r in results)
    assert results[0]["confidence"] >= 0.7

# tests/accuracy/test_benchmark.py 예시
def test_korean_cer_below_threshold(benchmark_dataset):
    total_cer = compute_cer(pipeline, benchmark_dataset["ko"])
    assert total_cer <= 0.05, f"CER {total_cer:.3f} exceeds 5% threshold"
```

**자동 실행 스크립트 (`scripts/run_tests.sh`)**

```bash
#!/bin/bash
set -e

echo "=== Phase 1 자체 테스트 시작 ==="

# 1. 단위 테스트 + 커버리지
pytest tests/unit/ -v --cov=core --cov-report=term-missing \
       --cov-fail-under=90

# 2. 통합 테스트
pytest tests/integration/ -v

# 3. 정확도 벤치마크 (모델 필요 시에만)
if [ "$RUN_ACCURACY" = "1" ]; then
  pytest tests/accuracy/ -v --benchmark-only
fi

echo "=== Phase 1 테스트 완료 ==="
```

**Phase 1 통과 기준 (배포 게이트)**
- [ ] 단위 테스트 전체 통과 (0 failures)
- [ ] 코드 커버리지 90% 이상
- [ ] 통합 테스트 전체 통과
- [ ] 한국어 CER ≤ 5%, 영어 CER ≤ 3%
- [ ] CLI 도구 smoke test 통과

#### 4.1.5 마일스톤
- W1-2: PaddleOCR 환경 구축, 기본 한국어/영어 인식 확인, fixture 이미지셋 준비
- W3-4: 전처리 파이프라인 구현 + 단위 테스트 병행 작성
- W5: JSON 출력 표준화, 배치 처리 구현, 통합 테스트 작성
- W6: 정확도 벤치마크 실행 → **Phase 1 게이트 통과 확인**

---

### Phase 2 — REST API 서버 (4주)

#### 4.2.1 목표
OCR 코어를 HTTP API로 노출. 동기·비동기 처리 모두 지원.

#### 4.2.2 디렉토리 구조 추가
```
ocr_model/
├── api/
│   ├── main.py              # FastAPI 앱 진입점
│   ├── routers/
│   │   ├── ocr.py           # /ocr 엔드포인트
│   │   └── health.py        # /health
│   ├── schemas/
│   │   ├── request.py       # Pydantic 요청 모델
│   │   └── response.py      # Pydantic 응답 모델
│   ├── services/
│   │   └── ocr_service.py   # 비즈니스 로직
│   ├── tasks/
│   │   └── ocr_task.py      # Celery 비동기 태스크
│   └── middleware/
│       ├── auth.py          # API Key 인증
│       └── rate_limit.py    # 요청 속도 제한
├── docker/
│   ├── Dockerfile
│   └── docker-compose.yml
└── .env.example
```

#### 4.2.3 API 엔드포인트 명세
```
POST /api/v1/ocr/image
  - 요청: multipart/form-data (image file)
  - 응답: { task_id, status, result: [{ text, bbox, confidence }] }
  - 처리: 동기 (소용량 이미지)

POST /api/v1/ocr/batch
  - 요청: multipart/form-data (files[])
  - 응답: { task_id } → 비동기 처리
  - 폴링: GET /api/v1/tasks/{task_id}

WebSocket /api/v1/ocr/stream
  - 카메라 프레임 실시간 전송 → 텍스트 결과 반환

GET /api/v1/tasks/{task_id}
  - 비동기 작업 상태 및 결과 조회

GET /api/v1/health
  - 서버 상태 확인
```

#### 4.2.4 테스트 전략 (Phase 2)

**사용 도구:** pytest, pytest-asyncio, httpx, Locust

**테스트 레이어**

| 레이어 | 대상 | 도구 | 목표 |
|--------|------|------|------|
| 단위 테스트 | 각 라우터·서비스·미들웨어 함수 | pytest | 85%+ 커버리지 |
| API 통합 테스트 | 실제 HTTP 요청/응답 검증 | pytest + httpx | 모든 엔드포인트 |
| 비동기 태스크 테스트 | Celery 태스크 실행 결과 검증 | pytest-asyncio | - |
| 부하 테스트 | 동시 요청 처리 한계 측정 | Locust | 50 req/s 유지 |
| 보안 테스트 | 인증 우회·비정상 입력 거부 확인 | pytest | - |

**주요 테스트 케이스**

```python
# tests/api/test_ocr_router.py 예시
async def test_image_upload_returns_result(client, sample_image_bytes):
    resp = await client.post("/api/v1/ocr/image",
                             files={"file": ("test.jpg", sample_image_bytes)})
    assert resp.status_code == 200
    data = resp.json()
    assert "result" in data
    assert len(data["result"]) > 0

async def test_invalid_file_type_rejected(client):
    resp = await client.post("/api/v1/ocr/image",
                             files={"file": ("test.exe", b"fake")})
    assert resp.status_code == 422

async def test_missing_api_key_returns_401(client):
    resp = await client.post("/api/v1/ocr/image",
                             headers={},
                             files={"file": ("t.jpg", b"...")})
    assert resp.status_code == 401

async def test_batch_task_eventually_completes(client, sample_images):
    resp = await client.post("/api/v1/ocr/batch", files=sample_images)
    task_id = resp.json()["task_id"]
    # 폴링으로 완료 확인 (최대 30초)
    for _ in range(30):
        r = await client.get(f"/api/v1/tasks/{task_id}")
        if r.json()["status"] == "done":
            break
        await asyncio.sleep(1)
    assert r.json()["status"] == "done"

# locustfile.py 예시
class OCRUser(HttpUser):
    @task
    def ocr_image(self):
        self.client.post("/api/v1/ocr/image",
                         files={"file": ("t.jpg", SAMPLE_BYTES)},
                         headers={"Authorization": f"Bearer {API_KEY}"})
```

**테스트 디렉토리 추가**
```
tests/
├── api/
│   ├── test_ocr_router.py       # 엔드포인트 통합 테스트
│   ├── test_health_router.py
│   ├── test_auth_middleware.py  # 인증 보안 테스트
│   └── test_rate_limit.py
├── tasks/
│   └── test_celery_tasks.py     # 비동기 태스크 테스트
└── load/
    └── locustfile.py            # 부하 테스트
```

**Phase 2 통과 기준 (배포 게이트)**
- [ ] 단위 + API 통합 테스트 전체 통과
- [ ] 코드 커버리지 85% 이상
- [ ] 401/422 보안 케이스 전체 통과
- [ ] Locust 50 req/s에서 에러율 1% 미만
- [ ] Phase 1 회귀 테스트 통과 (코어 테스트 재실행)

#### 4.2.5 마일스톤
- W7-8: FastAPI 기본 구조, 동기 이미지 엔드포인트, API 통합 테스트 작성
- W9: Celery + Redis 비동기 배치 처리, 태스크 테스트 작성
- W10: WebSocket·인증·Rate Limiting 구현 + 부하 테스트 → **Phase 2 게이트 통과 확인**

---

### Phase 3 — Web 애플리케이션 (5주)

#### 4.3.1 목표
브라우저에서 사진 업로드 및 결과 확인. 실시간 카메라 OCR (WebRTC).

#### 4.3.2 디렉토리 구조
```
ocr_model/
└── web/
    ├── src/
    │   ├── pages/
    │   │   ├── Home.tsx         # 메인 페이지
    │   │   ├── Upload.tsx       # 파일 업로드
    │   │   └── Camera.tsx       # 웹캠 실시간 OCR
    │   ├── components/
    │   │   ├── ImageDropzone/   # 드래그앤드롭 업로드
    │   │   ├── ResultViewer/    # OCR 결과 오버레이
    │   │   ├── CameraView/      # 웹캠 뷰어
    │   │   └── TextEditor/      # 결과 텍스트 편집
    │   ├── hooks/
    │   │   ├── useOCR.ts        # API 호출 훅
    │   │   └── useCamera.ts     # 웹캠 제어 훅
    │   ├── api/
    │   │   └── ocrClient.ts     # API 클라이언트
    │   └── types/
    │       └── ocr.ts           # TypeScript 타입 정의
    ├── public/
    ├── package.json
    └── vite.config.ts
```

#### 4.3.3 핵심 UI 기능
| 기능 | 상세 |
|------|------|
| 파일 업로드 | 드래그앤드롭, 클립보드 붙여넣기, 다중 파일 |
| 결과 오버레이 | 원본 이미지 위에 bounding box + 텍스트 표시 |
| 웹캠 실시간 OCR | WebSocket으로 프레임 전송, 실시간 텍스트 오버레이 |
| 텍스트 편집 | 인식 결과 수정 후 복사·다운로드 |
| 이력 관리 | 최근 인식 결과 로컬 저장 |
| 다국어 UI | 한국어/영어 인터페이스 전환 |

#### 4.3.4 테스트 전략 (Phase 3)

**사용 도구:** Vitest, React Testing Library, Playwright, MSW(Mock Service Worker)

**테스트 레이어**

| 레이어 | 대상 | 도구 | 목표 |
|--------|------|------|------|
| 단위 테스트 | hooks, utils, API 클라이언트 | Vitest | 85%+ 커버리지 |
| 컴포넌트 테스트 | 각 React 컴포넌트 렌더링·인터랙션 | React Testing Library | 주요 컴포넌트 100% |
| E2E 테스트 | 업로드→결과 확인 전체 흐름 | Playwright | 핵심 시나리오 |
| 크로스브라우저 | Chrome, Firefox, Safari(WebKit) | Playwright | 3개 브라우저 통과 |
| 접근성 | ARIA, 키보드 탐색 | axe-core (MIT) | 위반 0건 |

**주요 테스트 케이스**

```typescript
// tests/unit/useOCR.test.ts 예시
it('성공 응답 시 result 상태를 업데이트한다', async () => {
  server.use(rest.post('/api/v1/ocr/image', (req, res, ctx) =>
    res(ctx.json({ result: [{ text: '안녕', confidence: 0.99 }] }))
  ));
  const { result } = renderHook(() => useOCR());
  await act(() => result.current.analyze(mockFile));
  expect(result.current.results[0].text).toBe('안녕');
});

// tests/components/ImageDropzone.test.tsx 예시
it('이미지 파일 드롭 시 미리보기가 표시된다', async () => {
  render(<ImageDropzone onFile={vi.fn()} />);
  const file = new File([sampleBytes], 'test.jpg', { type: 'image/jpeg' });
  fireEvent.drop(screen.getByTestId('dropzone'), { dataTransfer: { files: [file] } });
  expect(await screen.findByAltText('preview')).toBeInTheDocument();
});

// tests/e2e/upload.spec.ts 예시 (Playwright)
test('파일 업로드 후 텍스트 결과가 화면에 표시된다', async ({ page }) => {
  await page.goto('/');
  await page.setInputFiles('input[type=file]', 'tests/fixtures/sample_ko.jpg');
  await expect(page.locator('[data-testid="result-text"]')).toBeVisible({ timeout: 10000 });
  const text = await page.locator('[data-testid="result-text"]').textContent();
  expect(text?.length).toBeGreaterThan(0);
});

test('웹캠 버튼 클릭 시 카메라 권한 요청이 발생한다', async ({ page }) => {
  await page.goto('/camera');
  await page.click('[data-testid="start-camera"]');
  // 권한 UI 또는 video 엘리먼트 등장 확인
  await expect(page.locator('video')).toBeVisible();
});
```

**테스트 디렉토리 추가**
```
web/
├── src/
└── tests/
    ├── unit/
    │   ├── useOCR.test.ts
    │   └── useCamera.test.ts
    ├── components/
    │   ├── ImageDropzone.test.tsx
    │   ├── ResultViewer.test.tsx
    │   └── CameraView.test.tsx
    ├── e2e/
    │   ├── upload.spec.ts          # 업로드 흐름 E2E
    │   ├── camera.spec.ts          # 웹캠 OCR E2E
    │   └── accessibility.spec.ts   # 접근성 검사
    └── mocks/
        └── handlers.ts             # MSW API 모킹
```

**package.json 테스트 명령**
```json
{
  "scripts": {
    "test":          "vitest run",
    "test:watch":    "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e":      "playwright test",
    "test:all":      "npm run test:coverage && npm run test:e2e"
  }
}
```

**Phase 3 통과 기준 (배포 게이트)**
- [ ] Vitest 단위+컴포넌트 테스트 전체 통과
- [ ] 코드 커버리지 85% 이상
- [ ] Playwright E2E — Chrome/Firefox/Safari 3개 브라우저 통과
- [ ] 접근성 위반 0건
- [ ] Phase 1+2 회귀 테스트 통과

#### 4.3.5 마일스톤
- W11-12: 프로젝트 구조, 업로드 페이지, API 연동, 단위+컴포넌트 테스트 작성
- W13: 결과 오버레이 컴포넌트, 텍스트 편집, 컴포넌트 테스트 완성
- W14: 웹캠 실시간 OCR (WebSocket), E2E 시나리오 작성
- W15: 반응형 디자인, 크로스브라우저 E2E → **Phase 3 게이트 통과 확인**

---

### Phase 4 — 모바일 앱 (6주)

#### 4.4.1 목표
iOS·Android 공용 앱. 카메라 실시간 OCR 및 갤러리 이미지 인식.

#### 4.4.2 디렉토리 구조
```
ocr_model/
└── mobile/
    ├── src/
    │   ├── screens/
    │   │   ├── HomeScreen.tsx
    │   │   ├── CameraScreen.tsx   # 실시간 카메라 OCR
    │   │   ├── GalleryScreen.tsx  # 갤러리 이미지 선택
    │   │   └── ResultScreen.tsx   # 결과 화면
    │   ├── components/
    │   │   ├── OCROverlay/        # 카메라 텍스트 오버레이
    │   │   ├── ResultCard/        # 인식 결과 카드
    │   │   └── LanguageSelector/  # 언어 선택
    │   ├── services/
    │   │   └── ocrService.ts      # API 통신
    │   ├── hooks/
    │   │   └── useOCR.ts
    │   └── navigation/
    │       └── AppNavigator.tsx
    ├── android/
    ├── ios/
    └── package.json
```

#### 4.4.3 핵심 모바일 기능
| 기능 | 상세 |
|------|------|
| 실시간 카메라 OCR | react-native-vision-camera로 프레임 캡처 후 API 전송 |
| 갤러리 이미지 OCR | 앨범에서 선택한 사진 인식 |
| 텍스트 복사·공유 | 시스템 클립보드, 공유 시트 연동 |
| 오프라인 모드 | 경량 ONNX 모델을 온디바이스 실행 (옵션) |
| 히스토리 | 로컬 DB에 인식 이력 저장 (SQLite) |
| 플래시 제어 | 어두운 환경 보조광 |

#### 4.4.4 테스트 전략 (Phase 4)

**사용 도구:** Jest, React Native Testing Library, Detox

**테스트 레이어**

| 레이어 | 대상 | 도구 | 목표 |
|--------|------|------|------|
| 단위 테스트 | 서비스·훅·유틸 함수 | Jest | 85%+ 커버리지 |
| 컴포넌트 테스트 | 각 스크린·컴포넌트 렌더링 | RN Testing Library | 주요 화면 100% |
| E2E 테스트 (Android) | 갤러리 선택→결과 표시 흐름 | Detox + Android Emulator | 핵심 시나리오 |
| E2E 테스트 (iOS) | 동일 시나리오 iOS 기기 | Detox + iOS Simulator | 핵심 시나리오 |
| 성능 테스트 | 실시간 OCR FPS, 메모리 사용 | React Native Performance | FPS ≥ 15, RAM ≤ 200MB |

**주요 테스트 케이스**

```typescript
// tests/unit/ocrService.test.ts 예시
it('API 성공 시 OCR 결과를 반환한다', async () => {
  fetchMock.mockResponseOnce(JSON.stringify({ result: [{ text: '테스트' }] }));
  const result = await ocrService.analyzeImage('file://test.jpg');
  expect(result[0].text).toBe('테스트');
});

it('네트워크 오류 시 OCRNetworkError를 던진다', async () => {
  fetchMock.mockReject(new Error('Network error'));
  await expect(ocrService.analyzeImage('file://test.jpg'))
    .rejects.toThrow(OCRNetworkError);
});

// tests/components/ResultScreen.test.tsx 예시
it('결과 화면에서 텍스트 복사 버튼이 동작한다', async () => {
  const { getByTestId } = render(
    <ResultScreen results={[{ text: '안녕하세요', confidence: 0.98 }]} />
  );
  fireEvent.press(getByTestId('copy-btn'));
  expect(Clipboard.setString).toHaveBeenCalledWith('안녕하세요');
});

// e2e/gallery_ocr.e2e.ts 예시 (Detox)
describe('갤러리 이미지 OCR', () => {
  it('갤러리에서 이미지 선택 후 결과 화면으로 이동한다', async () => {
    await element(by.id('gallery-btn')).tap();
    await element(by.id('gallery-item-0')).tap();
    await waitFor(element(by.id('result-text')))
      .toBeVisible().withTimeout(15000);
    await expect(element(by.id('result-text'))).toBeVisible();
  });
});
```

**테스트 디렉토리 추가**
```
mobile/
├── src/
└── __tests__/
    ├── unit/
    │   ├── ocrService.test.ts
    │   └── useOCR.test.ts
    ├── components/
    │   ├── OCROverlay.test.tsx
    │   ├── ResultCard.test.tsx
    │   └── ResultScreen.test.tsx
    └── e2e/
        ├── gallery_ocr.e2e.ts      # 갤러리 OCR 흐름
        ├── camera_ocr.e2e.ts       # 카메라 OCR 흐름
        └── history.e2e.ts          # 이력 화면
```

**package.json 테스트 명령**
```json
{
  "scripts": {
    "test":             "jest",
    "test:coverage":    "jest --coverage",
    "test:e2e:android": "detox test -c android.emu.debug",
    "test:e2e:ios":     "detox test -c ios.sim.debug",
    "test:all":         "npm run test:coverage && npm run test:e2e:android && npm run test:e2e:ios"
  }
}
```

**Phase 4 통과 기준 (배포 게이트)**
- [ ] Jest 단위+컴포넌트 테스트 전체 통과
- [ ] 코드 커버리지 85% 이상
- [ ] Detox E2E — Android Emulator 통과
- [ ] Detox E2E — iOS Simulator 통과
- [ ] 실시간 OCR FPS ≥ 15, 메모리 ≤ 200MB
- [ ] Phase 1+2+3 회귀 테스트 통과

#### 4.4.5 마일스톤
- W16-17: RN 셋업, 카메라 권한, 갤러리 OCR + 단위/컴포넌트 테스트
- W18-19: 실시간 카메라 OCR, 오버레이 렌더링, Detox E2E 작성
- W20: 히스토리, 공유, UX 다듬기, 성능 측정
- W21: iOS/Android 빌드, 스토어 배포 준비 → **Phase 4 게이트 통과 확인**

---

## 5. 기술 스택 요약

| 레이어 | 기술 | 라이선스 |
|--------|------|---------|
| OCR 엔진 | PaddleOCR (PP-OCRv4) | Apache 2.0 |
| 추론 최적화 | ONNXRuntime | MIT |
| 이미지 처리 | OpenCV, Pillow | Apache 2.0 / HPND |
| API 서버 | Python 3.11 + FastAPI | MIT |
| 비동기 큐 | Celery + Redis | MIT / BSD |
| DB | PostgreSQL + SQLAlchemy | PostgreSQL License / MIT |
| 파일 스토리지 | MinIO (S3 호환) | AGPL → 자체 호스팅 시 상용 무료\* |
| 웹 프론트 | React 18 + Vite + TypeScript | MIT |
| 모바일 | React Native 0.74 | MIT |
| 컨테이너 | Docker + Docker Compose | Apache 2.0 |
| CI/CD | GitHub Actions | 무료 플랜 |
| 테스트 (Python) | pytest, pytest-cov, pytest-asyncio, Hypothesis, Locust | MIT / MPL-2.0 |
| 테스트 (Web) | Vitest, React Testing Library, Playwright, MSW | MIT / Apache 2.0 |
| 테스트 (Mobile) | Jest, React Native Testing Library, Detox | MIT |

> \* MinIO는 AGPL이나, **자체 서버에서 내부 스토리지로만 사용**하면 상용 제품 소스 공개 의무 없음.  
> 고객에게 MinIO 자체를 SaaS로 제공하는 경우에는 상용 라이선스 구매 필요.  
> 대안: Cloudflare R2 (무료 티어) 또는 AWS S3 사용 시 라이선스 이슈 없음.

---

## 6. 테스트 자동화 전략 (CI/CD)

### 6.1 테스트 도구 전체 요약

| Phase | 단위 테스트 | 통합/E2E | 부하/성능 | 커버리지 목표 |
|-------|------------|---------|---------|-------------|
| 1 — 코어 | pytest + Hypothesis | pytest (pipeline) | pytest-benchmark | 90%+ |
| 2 — API | pytest-asyncio | httpx 통합 테스트 | Locust | 85%+ |
| 3 — Web | Vitest + RTL | Playwright (3 브라우저) | - | 85%+ |
| 4 — Mobile | Jest + RNTL | Detox (Android/iOS) | RN Performance | 85%+ |

### 6.2 GitHub Actions CI 파이프라인

각 Phase는 PR/push 시 아래 워크플로우가 자동 실행된다.

```yaml
# .github/workflows/ci.yml

name: CI

on: [push, pull_request]

jobs:
  # ── Phase 1: OCR 코어 테스트 ──────────────────────────────────
  test-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install -r requirements.txt
      - name: 단위 테스트 + 커버리지
        run: |
          pytest tests/unit/ -v \
            --cov=core --cov-report=xml \
            --cov-fail-under=90
      - name: 통합 테스트
        run: pytest tests/integration/ -v
      - uses: codecov/codecov-action@v4

  # ── Phase 2: API 서버 테스트 ──────────────────────────────────
  test-api:
    runs-on: ubuntu-latest
    needs: test-core             # 코어 통과 후 실행
    services:
      redis:
        image: redis:7-alpine
        ports: ['6379:6379']
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: ocr_test
          POSTGRES_PASSWORD: test
        ports: ['5432:5432']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install -r requirements.txt
      - name: API 통합 테스트
        run: pytest tests/api/ -v --cov=api --cov-fail-under=85
      - name: 비동기 태스크 테스트
        run: pytest tests/tasks/ -v

  # ── Phase 3: Web 테스트 ───────────────────────────────────────
  test-web:
    runs-on: ubuntu-latest
    needs: test-api
    defaults:
      run: { working-directory: web }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - name: 단위·컴포넌트 테스트
        run: npm run test:coverage
      - name: Playwright E2E (3 브라우저)
        run: |
          npx playwright install --with-deps
          npm run test:e2e

  # ── Phase 4: 모바일 테스트 ────────────────────────────────────
  test-mobile-android:
    runs-on: ubuntu-latest
    needs: test-api
    defaults:
      run: { working-directory: mobile }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - name: Jest 단위·컴포넌트 테스트
        run: npm run test:coverage
      - name: Detox E2E (Android Emulator)
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 33
          script: cd mobile && npm run test:e2e:android

  test-mobile-ios:
    runs-on: macos-14           # iOS Simulator는 macOS runner 필요
    needs: test-api
    defaults:
      run: { working-directory: mobile }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci && cd ios && pod install
      - name: Detox E2E (iOS Simulator)
        run: npm run test:e2e:ios
```

### 6.3 Phase 게이트 — 단계별 통과 기준 한눈에 보기

```
PR Merge 조건
─────────────────────────────────────────────────────────────
Phase 1  ✅ pytest 단위+통합 통과  ✅ 커버리지 ≥ 90%  ✅ CER ≤ 5%
          ↓ 통과 후 Phase 2 개발 시작
Phase 2  ✅ API 통합 테스트 통과  ✅ 커버리지 ≥ 85%  ✅ Locust 50rps
         ✅ 보안(401/422) 케이스 통과
         ✅ Phase 1 회귀 테스트 통과
          ↓ 통과 후 Phase 3 개발 시작
Phase 3  ✅ Vitest 통과  ✅ 커버리지 ≥ 85%
         ✅ Playwright 3 브라우저 통과  ✅ 접근성 위반 0건
         ✅ Phase 1+2 회귀 테스트 통과
          ↓ 통과 후 Phase 4 개발 시작
Phase 4  ✅ Jest 통과  ✅ 커버리지 ≥ 85%
         ✅ Detox Android + iOS 통과  ✅ FPS ≥ 15
         ✅ Phase 1+2+3 회귀 테스트 통과
─────────────────────────────────────────────────────────────
```

### 6.4 로컬 전체 테스트 실행 (원클릭)

```bash
# scripts/test_all.sh — 모든 Phase 테스트를 로컬에서 순차 실행
#!/bin/bash
set -e

echo "▶ Phase 1 — OCR 코어"
pytest tests/unit/ tests/integration/ -v --cov=core --cov-fail-under=90

echo "▶ Phase 2 — API 서버 (Docker Compose 필요)"
docker compose -f docker/docker-compose.test.yml up -d
pytest tests/api/ tests/tasks/ -v --cov=api --cov-fail-under=85
docker compose -f docker/docker-compose.test.yml down

echo "▶ Phase 3 — Web"
cd web && npm run test:all && cd ..

echo "▶ Phase 4 — Mobile"
cd mobile && npm run test:coverage && cd ..

echo "✅ 전체 테스트 완료"
```

---

## 7. 데이터 모델


```sql
-- 인식 작업 이력
CREATE TABLE ocr_jobs (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    status      VARCHAR(20) NOT NULL,   -- pending/processing/done/failed
    image_url   TEXT,
    language    VARCHAR(10),
    created_at  TIMESTAMPTZ DEFAULT now(),
    finished_at TIMESTAMPTZ
);

-- 인식 결과 (텍스트 블록별)
CREATE TABLE ocr_results (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    job_id      UUID REFERENCES ocr_jobs(id),
    text        TEXT NOT NULL,
    confidence  FLOAT,
    bbox        JSONB,                  -- [[x1,y1],[x2,y2],[x3,y3],[x4,y4]]
    page_num    INT DEFAULT 1
);

-- API 키 관리
CREATE TABLE api_keys (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    key_hash    TEXT UNIQUE NOT NULL,
    name        VARCHAR(100),
    rate_limit  INT DEFAULT 100,        -- 분당 요청 수
    is_active   BOOLEAN DEFAULT true,
    created_at  TIMESTAMPTZ DEFAULT now()
);
```

---

## 8. 성능 목표

| 지표 | 목표값 |
|------|-------|
| 한국어 인식 정확도 (CER) | ≤ 5% (95%+) |
| 영어 인식 정확도 | ≤ 3% |
| 단일 이미지 처리 시간 (API) | ≤ 2초 (CPU), ≤ 0.5초 (GPU) |
| 실시간 스트림 지연 | ≤ 200ms |
| API 동시 처리 | 50 req/s (단일 서버 기준) |
| 모바일 온디바이스 처리 | ≤ 1초 (ONNX 경량 모델) |

---

## 9. 보안 설계

- **API 인증:** API Key (Bearer Token) — Phase 2부터 적용
- **입력 검증:** 파일 타입·크기 제한 (최대 10MB, JPEG/PNG/PDF만 허용)
- **Rate Limiting:** IP 및 API Key 기준 분당 요청 수 제한
- **저장 데이터:** 업로드 이미지는 처리 후 24시간 내 자동 삭제 (GDPR 대응)
- **전송 암호화:** HTTPS/TLS 1.3 필수
- **SQL Injection 방지:** SQLAlchemy ORM 사용, raw query 금지

---

## 10. 전체 일정

```
                       W1  W2  W3  W4  W5  W6  W7  W8  W9  W10 W11 W12 W13 W14 W15 W16 W17 W18 W19 W20 W21
Phase 1 — OCR 코어    [██  ██  ██  ██  ██  ██  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░]
  테스트 (코어)        [░░  ░░  ▒▒  ▒▒  ▒▒  ✓   ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░]
Phase 2 — API 서버    [░░  ░░  ░░  ░░  ░░  ░░  ██  ██  ██  ██  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░]
  테스트 (API)         [░░  ░░  ░░  ░░  ░░  ░░  ▒▒  ▒▒  ▒▒  ✓   ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░]
Phase 3 — Web App     [░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ██  ██  ██  ██  ██  ░░  ░░  ░░  ░░  ░░  ░░]
  테스트 (Web)         [░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ▒▒  ▒▒  ▒▒  ▒▒  ✓   ░░  ░░  ░░  ░░  ░░  ░░]
Phase 4 — Mobile App  [░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ██  ██  ██  ██  ██  ██]
  테스트 (Mobile)      [░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ░░  ▒▒  ▒▒  ▒▒  ▒▒  ▒▒  ✓ ]
회귀 테스트 (CI)       [░░  ░░  ░░  ░░  ░░  ░░  R1  R1  R1  R1  R2  R2  R2  R2  R2  R3  R3  R3  R3  R3  R3]

██ = 기능 개발   ▒▒ = 테스트 작성   ✓ = Phase 게이트   R1/R2/R3 = 이전 Phase 회귀 테스트
```

**총 예상 기간: 약 21주 (5개월)**

---

## 11. 주요 리스크 및 대응

| 리스크 | 가능성 | 대응 방안 |
|--------|--------|---------|
| 한국어 인식 정확도 미달 | 중 | PaddleOCR fine-tuning 또는 EasyOCR 병행 검토 |
| GPU 서버 비용 과다 | 중 | ONNXRuntime CPU 최적화, 클라우드 GPU 필요 시 spot instance |
| 모바일 카메라 성능 이슈 | 중 | 프레임 전송 주기 조절, 온디바이스 ONNX 경량 모델 대안 |
| PaddleOCR 라이선스 변경 | 저 | EasyOCR(Apache 2.0)을 즉시 대체 엔진으로 확보 |
| MinIO AGPL 이슈 | 저 | 초기부터 AWS S3/Cloudflare R2 대체 설계 |

---

## 12. 추후 확장 계획

- **PDF·문서 OCR:** PyMuPDF(AGPL 주의 → 대안: pdfplumber MIT) 활용
- **표·레이아웃 분석:** 표 구조 인식, 엑셀 변환
- **손글씨 인식:** 별도 handwriting 모델 추가
- **번역 연동:** DeepL API 또는 LibreTranslate(AGPLv3 → 자체 호스팅)
- **SaaS 과금:** Stripe 연동, 사용량 기반 과금

---

*본 문서는 프로젝트 진행에 따라 지속 업데이트됩니다.*
